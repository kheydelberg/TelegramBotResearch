# TelegramBotResearch

Это ветка Ворогушина Максима.

Команды, чтобы все заработало:
pip install -U aiogram
pip install environs
pip install apscheduler
pip install aiomysql
pip install redis
pip install apscheduler-di